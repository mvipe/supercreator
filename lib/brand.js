// =============================================================
// MegaProfile — public-facing brand.
//
// Every creator-facing page (stores, product pages, checkout) shows this
// name, so the whole rebrand lives in one place. The dashboard/admin keep
// their own naming; change BRAND.name here and every public surface follows.
// =============================================================

export const BRAND = {
  name: "SuperCreators",
  // Split so the badge can render "Super" light + "Creators" bold, like the
  // reference lockup.
  namePrefix: "Super",
  nameSuffix: "Creators",
  url: "/",
  tagline: "Sell courses, sessions and digital products from one link."
};